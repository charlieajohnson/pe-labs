# Security

Please report vulnerabilities privately through GitHub's security advisory form. Do not open a public issue with exploit details.

The public deployment uses synthetic data. API credentials belong only in Vercel environment variables and must never be committed or exposed through `NEXT_PUBLIC_` variables.
